// lib/sentry.ts
import * as Sentry from '@sentry/react-native';

// This file exports the Sentry instance that's initialized in app/_layout.tsx
// The actual initialization happens in app/_layout.tsx via the Sentry wizard

export default Sentry;
